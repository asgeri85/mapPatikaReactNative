import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    alignSelf: 'center',
    bottom: 300,
    backgroundColor: 'white',
    borderRadius: 15,
    padding: 5,
  },
});
